# Important commands:

1. ```python -m venv drone_env```
2. ```Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser```
3. ```drone_env\Scripts\activate```
4. ```pip install -r requirements.txt```
